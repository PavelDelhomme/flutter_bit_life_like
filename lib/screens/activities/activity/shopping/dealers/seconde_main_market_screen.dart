import 'package:flutter/material.dart';

class SecondHandMarketScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Second Hand Market'),
      ),
      body: Center(
        child: Text('Second Hand Market Screen Content'),
      ),
    );
  }
}
