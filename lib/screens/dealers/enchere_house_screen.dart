import 'package:flutter/material.dart';

class EnchereHouseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Auction House'),
      ),
      body: Center(
        child: Text('Auction House Screen Content'),
      ),
    );
  }
}
