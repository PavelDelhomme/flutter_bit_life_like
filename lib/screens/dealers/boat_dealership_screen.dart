import 'package:flutter/material.dart';

class BoatDealershipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Boat Dealerships'),
      ),
      body: Center(
        child: Text('Boat Dealership Screen Content'),
      ),
    );
  }
}
