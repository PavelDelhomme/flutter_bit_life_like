import 'package:flutter/material.dart';

class CarDealershipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Car Dealerships'),
      ),
      body: Center(
        child: Text('Car Dealership Screen Content'),
      ),
    );
  }
}
